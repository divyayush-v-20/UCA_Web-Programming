//1. Local storage
//  setItem
//  getItem
//  removeItem

// localStorage.setItem("name", "divyayush"); //values always in string

// localStorage.getItem("name");

// localStorage.removeItem("name");

// local storage keeps stored data to the current domain, till it is not removed
// or the browser cache has not been cleared

//2. Session storage

// sessionStorage.setItem("sessionName", "sessionName1");

// sessionStorage.getItem("sessionName");

// sessionStorage.removeItem("sessionName");

// session storage stores data till the current session goes on,
// storage is cleared as soon as the session ends